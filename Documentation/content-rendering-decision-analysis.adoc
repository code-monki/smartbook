= Content Rendering Architecture Decision Analysis
:author: AI Assistant
:revdate: 2025-12-26
:doctype: article
:toc: left
:toclevels: 3
:sectnums:

== Executive Summary

This document provides a comprehensive analysis of the architectural decision between **Qt WebEngine** and **QTextDocument/QTextBrowser** for content rendering in the SmartBook Reader application. This decision impacts Phase 1 implementation and all future phases.

== Requirements Analysis

=== Core Requirements (FR-2.2.x)

* **FR-2.2.1:** HTML/CSS rendering via standards-compliant web view
* **FR-2.2.2:** Embedded JavaScript application support (SPAs) - **CRITICAL REQUIREMENT**
* **FR-2.2.3:** Cartridge settings support (author-defined rendering preferences)
* **FR-2.2.4:** Settings reset functionality

=== Form Requirements (FR-2.7.x)

* **FR-2.7.1 through FR-2.7.11:** Form rendering, validation, persistence, undo/redo
* Forms are defined as JSON schemas and rendered dynamically
* Forms require JavaScript execution for validation and interactivity

=== Creator Tool Requirements (FR-CT-3.x)

* **FR-CT-3.1:** WYSIWYG HTML content authoring
* **FR-CT-3.2:** Rich text editing with formatting toolbar
* **FR-CT-3.3:** Direct HTML editing mode
* **FR-CT-3.5:** Preview functionality

=== WebEngine Configuration Requirements (FR-WE-5.x)

* **FR-WE-5.1 through FR-WE-5.10:** Extensive WebEngine profile configuration requirements
* Network interception for embedded apps
* CSP header configuration
* WebChannel bridge for JavaScript/C++ communication

== Option 1: Qt WebEngine (Current Implementation)

=== Overview

Qt WebEngine is a Chromium-based web rendering engine that provides full HTML5/CSS3/JavaScript support.

=== Advantages

==== Feature Completeness
* ✅ **Full HTML5/CSS3 Support:** Complete standards compliance (95%+ HTML5, 90%+ CSS3)
* ✅ **JavaScript Execution:** Full V8 JavaScript engine support
* ✅ **Embedded Applications:** Native support for SPAs and interactive JavaScript applications
* ✅ **Form Rendering:** Can render forms defined in JSON schemas using JavaScript
* ✅ **WYSIWYG Editing:** Rich text editing with contentEditable API
* ✅ **WebChannel Bridge:** Built-in secure JavaScript/C++ communication

==== Security
* ✅ **Network Interception:** `QWebEngineUrlRequestInterceptor` for blocking network requests
* ✅ **CSP Support:** Content Security Policy headers for embedded apps
* ✅ **Process Isolation:** Chromium's multi-process architecture provides isolation
* ✅ **Sandboxing:** Can leverage Chromium's sandboxing features

==== Performance
* ✅ **Hardware Acceleration:** GPU-accelerated rendering
* ✅ **Modern Rendering:** Uses Chromium's Blink rendering engine
* ✅ **Caching:** Built-in HTTP cache and resource caching

=== Disadvantages

==== Deployment Complexity
* ❌ **Large Binary Size:** Chromium engine adds significant size (~100-200MB)
* ❌ **Runtime Dependencies:** Requires Qt WebEngine runtime components
* ❌ **Platform-Specific Builds:** Different builds for different platforms
* ❌ **License Complexity:** Chromium has complex licensing (BSD-style, but large codebase)

==== Resource Usage
* ❌ **Memory Footprint:** Chromium processes consume significant memory
* ❌ **Startup Time:** WebEngine initialization can be slower
* ❌ **Process Overhead:** Multiple processes for security isolation

==== Integration Challenges
* ❌ **Qt Widgets Integration:** WebEngine views are separate from Qt Widgets hierarchy
* ❌ **Styling Consistency:** Web content styling may not match native Qt widgets
* ❌ **Event Handling:** Some Qt events may not propagate correctly to WebEngine

==== Known Issues (from other projects)
* ⚠️ **Hybrid Approach Concerns:** Mentioned in architecture-notes.adoc - "hybrid approach may not be viable"
* ⚠️ **Platform-Specific Bugs:** Chromium bugs can affect Qt WebEngine
* ⚠️ **Version Dependencies:** Tied to specific Chromium/V8 versions

=== Implementation Status

* ✅ **Reader View:** `ReaderViewWindow` uses `QWebEngineView` (implemented)
* ✅ **WebChannel Bridge:** `WebChannelBridge` implemented for JavaScript/C++ communication
* ✅ **Creator Tool:** `ContentEditor` uses `QWebEngineView` for WYSIWYG editing (implemented)
* ✅ **Security Infrastructure:** Network interceptor, CSP headers (designed, partially implemented)

== Option 2: QTextDocument/QTextBrowser

=== Overview

QTextDocument is Qt's native rich text document framework. QTextBrowser is a read-only widget for displaying QTextDocument content.

=== Advantages

==== Deployment Simplicity
* ✅ **Lightweight:** No Chromium dependency, smaller binary size
* ✅ **Native Qt:** Fully integrated with Qt Widgets
* ✅ **Simple Deployment:** No additional runtime components
* ✅ **Consistent Styling:** Native Qt styling, matches application UI

==== Performance
* ✅ **Fast Startup:** No heavy engine initialization
* ✅ **Lower Memory:** Significantly lower memory footprint
* ✅ **Native Rendering:** Uses Qt's native rendering pipeline

==== Integration
* ✅ **Qt Widgets Integration:** Seamless integration with Qt Widgets
* ✅ **Event Handling:** Full Qt event system support
* ✅ **Styling:** Can use Qt stylesheets consistently

=== Disadvantages

==== Feature Limitations
* ❌ **Limited HTML Support:** Subset of HTML4, not HTML5
* ❌ **Limited CSS Support:** Basic CSS only, no CSS3 features (flexbox, grid, transforms, etc.)
* ❌ **No JavaScript:** Cannot execute JavaScript at all
* ❌ **No Embedded Applications:** Cannot run SPAs or interactive JavaScript apps
* ❌ **Limited Form Support:** Would need Qt Widgets-based form rendering (completely different approach)

==== Creator Tool Impact
* ❌ **No WYSIWYG Editing:** QTextDocument has limited editing capabilities
* ❌ **No Rich Text Toolbar:** Would need custom implementation
* ❌ **Limited Preview:** Cannot preview HTML5/CSS3 features

==== Requirements Gaps
* ❌ **FR-2.2.2 (Embedded Apps):** Cannot be implemented - would require complete redesign
* ❌ **FR-2.7.x (Forms):** Would need Qt Widgets forms instead of JSON schema forms
* ❌ **FR-WE-5.x (WebEngine Config):** Not applicable - different architecture needed

=== Implementation Requirements

If using QTextDocument:

* **Form Rendering:** Would need to:
  * Parse JSON form schemas
  * Generate Qt Widgets (QLineEdit, QComboBox, etc.) dynamically
  * Implement validation in C++ instead of JavaScript
  * Handle form data persistence differently
  * Implement undo/redo for Qt Widgets

* **Embedded Applications:** Would need to:
  * Completely redesign embedded app architecture
  * Use Qt Quick/QML instead of JavaScript SPAs
  * Or abandon embedded application feature entirely

* **Creator Tool:** Would need to:
  * Build custom WYSIWYG editor using QTextEdit
  * Implement rich text formatting manually
  * Limited HTML/CSS preview capabilities

== Critical Requirement Analysis

=== FR-2.2.2: Embedded JavaScript Applications

**Status:** **MANDATORY REQUIREMENT**

**Qt WebEngine:** ✅ Fully supported - this is exactly what WebEngine is designed for

**QTextDocument:** ❌ **NOT POSSIBLE** - No JavaScript execution capability

**Impact:** If we choose QTextDocument, we **cannot** implement embedded JavaScript applications as specified. This would require:
* Removing FR-2.2.2 from requirements, OR
* Complete redesign using QML/Qt Quick instead of JavaScript, OR
* Hybrid approach (QTextDocument for content, separate WebEngine for embedded apps)

=== FR-2.7.x: Form Rendering

**Status:** **MANDATORY REQUIREMENT**

**Qt WebEngine:** ✅ Can render forms from JSON schemas using JavaScript form libraries

**QTextDocument:** ⚠️ **REQUIRES REDESIGN** - Would need:
* JSON schema parser in C++
* Dynamic Qt Widgets generation
* C++ validation logic instead of JavaScript
* Different form data persistence mechanism
* Different undo/redo implementation

**Impact:** Significant additional development effort, different architecture

=== FR-CT-3.1: WYSIWYG Editing

**Status:** **MANDATORY REQUIREMENT**

**Qt WebEngine:** ✅ Full WYSIWYG with contentEditable API

**QTextDocument:** ⚠️ **LIMITED** - QTextEdit provides basic rich text, but:
* Limited HTML support
* No CSS3 features
* Would need custom implementation for advanced features

== Hybrid Approach Analysis

=== Option 3: Hybrid (QTextDocument + WebEngine)

**Concept:** Use QTextDocument for simple text content, WebEngine for rich content and embedded apps.

=== Advantages
* ✅ Best of both worlds (theoretically)
* ✅ Lighter weight for simple content
* ✅ Full features for rich content

=== Disadvantages
* ❌ **Complexity:** Two rendering systems to maintain
* ❌ **Consistency:** Different rendering engines may produce different results
* ❌ **Decision Logic:** Need to decide which engine to use for each content type
* ❌ **Context Switching:** Users may notice differences between simple and rich content
* ⚠️ **Architecture Notes Warning:** "hybrid approach may not be viable" (from other project experience)

=== Implementation Challenges
* How to determine which engine to use?
* How to handle content that starts simple but becomes rich?
* How to maintain consistent styling between engines?
* How to handle embedded apps in QTextDocument content?
* How to handle forms in QTextDocument content?

== Decision Matrix

[cols="2,1,1,1"]
|===
| Requirement | Qt WebEngine | QTextDocument | Hybrid

| FR-2.2.1 (HTML/CSS Rendering) | ✅ Full | ⚠️ Limited | ⚠️ Complex
| FR-2.2.2 (Embedded Apps) | ✅ Native | ❌ Impossible | ⚠️ Complex
| FR-2.7.x (Forms) | ✅ JSON Schema | ⚠️ Redesign | ⚠️ Complex
| FR-CT-3.1 (WYSIWYG) | ✅ Full | ⚠️ Limited | ⚠️ Complex
| Deployment Simplicity | ❌ Complex | ✅ Simple | ❌ Complex
| Memory Footprint | ❌ High | ✅ Low | ⚠️ Medium
| Performance (Text) | ⚠️ Good | ✅ Excellent | ⚠️ Varies
| Performance (Rich) | ✅ Excellent | ❌ Poor | ⚠️ Varies
| Code Complexity | ⚠️ Medium | ⚠️ Medium | ❌ High
| Maintenance Burden | ⚠️ Medium | ✅ Low | ❌ High
|===

== Recommendation

=== Primary Recommendation: **Qt WebEngine**

**Rationale:**

1. **Mandatory Requirements:** FR-2.2.2 (Embedded JavaScript Applications) **cannot** be implemented with QTextDocument. This is a core feature of SmartBook.

2. **Form Requirements:** FR-2.7.x forms are designed around JSON schemas and JavaScript validation. QTextDocument would require complete redesign.

3. **Creator Tool:** WYSIWYG editing with full HTML5/CSS3 support requires WebEngine.

4. **Already Implemented:** Significant infrastructure already built on WebEngine (ReaderViewWindow, ContentEditor, WebChannelBridge).

5. **Consistency:** Single rendering engine provides consistent experience.

=== Mitigation Strategies for WebEngine Disadvantages

==== Deployment Complexity
* **Solution:** Provide clear installation instructions
* **Solution:** Consider bundling WebEngine runtime
* **Solution:** Document platform-specific requirements

==== Resource Usage
* **Solution:** Profile isolation (already designed)
* **Solution:** Lazy loading of WebEngine views
* **Solution:** Memory limits for embedded apps

==== Integration Challenges
* **Solution:** Careful event handling design
* **Solution:** Consistent styling guidelines
* **Solution:** WebChannel bridge for communication

=== Alternative Recommendation: **QTextDocument** (Only if Embedded Apps Removed)

**Condition:** Only viable if FR-2.2.2 (Embedded JavaScript Applications) is removed from requirements.

**Impact:**
* Would require removing embedded application feature
* Would require redesigning form system
* Would require rebuilding Creator Tool WYSIWYG editor
* Would require significant Phase 1 rework

**Not Recommended** due to:
* Loss of core differentiating feature (embedded apps)
* Significant rework of existing implementation
* Reduced feature set

=== Not Recommended: **Hybrid Approach**

**Rationale:**
* Architecture notes indicate "hybrid approach may not be viable"
* High complexity for questionable benefit
* Maintenance burden of two rendering systems
* Consistency challenges

== Qt Alternatives for Embedded Applications

=== Option: Qt Quick/QML

**Overview:** Qt Quick/QML is Qt's declarative UI framework that can be embedded in Qt Widgets applications.

**Capabilities:**
* ✅ **Interactive UIs:** Can create interactive applications without full WebEngine
* ✅ **Embedded in Widgets:** `QQuickWidget` allows embedding QML in Qt Widgets applications
* ✅ **JavaScript Expressions:** QML supports JavaScript expressions (but still JavaScript)
* ✅ **Native Integration:** Better integration with Qt Widgets than WebEngine
* ✅ **Lighter Weight:** No Chromium dependency, smaller footprint than WebEngine
* ✅ **Synchronous Rendering:** Native Qt rendering, no separate process

**Limitations:**
* ⚠️ **Still Uses JavaScript:** QML expressions are JavaScript-based
* ⚠️ **Different Syntax:** QML is not HTML/CSS - different authoring model
* ⚠️ **Learning Curve:** Authors would need to learn QML instead of web technologies
* ⚠️ **Not Web Standards:** Cannot use existing web frameworks (React, Vue, etc.)

**Implementation Approach:**
* Store QML code in `Embedded_Apps` table instead of HTML/JS
* Use `QQuickWidget` to embed QML applications in content pages
* Provide QML authoring tools in Creator Tool
* Use Qt's property binding for C++/QML communication instead of WebChannel

**Feasibility:** ✅ **VIABLE ALTERNATIVE**

=== Option: Qt Widgets

**Overview:** Native Qt Widgets for embedded interactive components.

**Capabilities:**
* ✅ **Fully Native:** Complete Qt Widgets integration
* ✅ **No JavaScript:** Pure C++ implementation
* ✅ **Synchronous Rendering:** No separate process, no async issues
* ✅ **Consistent Styling:** Native Qt styling

**Limitations:**
* ❌ **Not "Embedded Apps":** These are just native widgets, not separate applications
* ❌ **Authoring Complexity:** Authors would need to write C++ or use a widget builder
* ❌ **Limited Flexibility:** Less flexible than QML or web technologies
* ❌ **Distribution:** Cannot distribute widget code in cartridges easily

**Feasibility:** ⚠️ **NOT RECOMMENDED** - Too restrictive for embedded applications

== Known Issues: Qt WebEngine Rendering Problems

=== Issue: Theme Change Rendering Flash

**Problem Description (from other project):**
* Chromium engine runs in a **separate process**
* Viewport updates are **asynchronous**
* When themes change:
  * **Long visible flash** during rendering
  * **Application shell renders several seconds before content viewport re-renders**
  * Creates jarring user experience

**Root Cause:**
* WebEngine's multi-process architecture
* IPC (Inter-Process Communication) delay between main process and renderer process
* Theme changes require full re-render in Chromium process
* No synchronization mechanism between Qt Widgets and Chromium rendering

**Impact on SmartBook:**
* ⚠️ **Settings Application (FR-2.2.3):** Theme changes would cause visible flash
* ⚠️ **User Experience:** Jarring transitions when changing rendering settings
* ⚠️ **Performance Perception:** Users see shell update before content, appears slow

**Potential Mitigations:**
* **Pre-render:** Pre-render content with new theme before displaying
* **Fade Transition:** Use fade effect to mask the flash
* **Cache Themes:** Pre-cache common theme combinations
* **Synchronization:** Attempt to synchronize theme changes (may not be possible)

**Feasibility of Mitigation:** ⚠️ **UNCERTAIN** - May be fundamental limitation

== Updated Requirements Analysis

=== FR-2.2.2: Embedded Applications (REVISED)

**Original Requirement:** Embedded JavaScript applications (SPAs)

**Alternative Options:**
1. **Qt Quick/QML Applications:** ✅ Viable alternative
   * Use QML instead of HTML/JavaScript
   * Embed via `QQuickWidget`
   * Better integration, no separate process
   * Still supports JavaScript expressions

2. **Qt Widgets Components:** ⚠️ Possible but restrictive
   * Native widgets embedded in content
   * Requires C++ authoring or widget builder
   * Less flexible

**Impact on Decision:**
* If QML is acceptable, **QTextDocument becomes more viable**
* QML can be embedded alongside QTextDocument content
* No need for full WebEngine for embedded apps

=== FR-2.7.x: Form Rendering (REVISED)

**User Input:** Qt Widget Forms are acceptable

**Impact:**
* ✅ **QTextDocument is viable** for form rendering
* Can parse JSON schemas and generate Qt Widgets
* C++ validation instead of JavaScript
* Native Qt integration

**Implementation:**
* Parse JSON form schema in C++
* Dynamically create Qt Widgets (QLineEdit, QComboBox, QCheckBox, etc.)
* Implement validation in C++
* Use Qt's undo/redo framework for form fields

== Updated Decision Matrix

[cols="2,1,1,1,1"]
|===
| Requirement | Qt WebEngine | QTextDocument + QML | QTextDocument Only | Notes

| FR-2.2.1 (HTML/CSS) | ✅ Full | ⚠️ Limited HTML | ⚠️ Limited HTML | QML different syntax
| FR-2.2.2 (Embedded Apps) | ✅ JavaScript SPAs | ✅ QML Apps | ❌ Not Supported | QML viable alternative
| FR-2.7.x (Forms) | ✅ JSON + JS | ✅ Qt Widgets | ✅ Qt Widgets | Widgets acceptable
| FR-CT-3.1 (WYSIWYG) | ✅ Full | ⚠️ Limited | ⚠️ Limited | 
| Theme Change Flash | ❌ **Known Issue** | ✅ No Flash | ✅ No Flash | **Critical Issue**
| Deployment | ❌ Complex | ✅ Simple | ✅ Simple | 
| Memory | ❌ High | ✅ Low | ✅ Low | 
| Performance (Text) | ⚠️ Good | ✅ Excellent | ✅ Excellent | 
| Code Complexity | ⚠️ Medium | ⚠️ Medium | ✅ Low | 
|===

== Updated Recommendations

=== Final Decision: **QTextDocument + Qt Quick/QML** ✅

**Status:** **ACCEPTED**

**Rationale:**

1. **Rendering Flash:** ❌ **UNACCEPTABLE** - Deal-breaker for WebEngine. Other project spent significant effort trying to resolve and was unsuccessful.

2. **Embedded Apps Alternative:** ✅ Qt Quick/QML provides equivalent functionality:
   * Can be embedded via `QQuickWidget`
   * No separate process (synchronous rendering, no flash)
   * Better Qt integration
   * Supports JavaScript expressions for interactivity
   * **User Confirmed:** QML acceptable if it provides same functionality as JavaScript

3. **HTML/CSS Level:** ✅ HTML4 + CSS 2.1 sufficient:
   * **User Confirmed:** HTML4 + basic CSS acceptable
   * Qt has **no plans** to add HTML5/CSS3 support to QTextDocument
   * With robust WYSIWYG authoring environment, main loss is advanced CSS3 functionality

4. **Forms:** ✅ Qt Widget Forms acceptable:
   * **User Confirmed:** Qt Widget Forms acceptable
   * Can parse JSON schemas and generate Qt Widgets
   * C++ validation instead of JavaScript

5. **WYSIWYG:** ⚠️ Limited but acceptable:
   * **User Input:** Depends on HOW limited
   * QTextEdit provides: bold, italic, underline, font size, font family, colors, lists, alignment
   * Cannot preview CSS3 features
   * With robust authoring environment, limitations may be acceptable

6. **Deployment:** ✅ Simpler deployment without Chromium dependency

7. **Performance:** ✅ Better performance for text-heavy content, no async rendering issues

**Implementation Approach:**

* **Content Rendering:** `QTextBrowser` for HTML4 + CSS 2.1 content
* **Embedded Apps:** QML applications embedded via `QQuickWidget` in content pages
* **Forms:** Qt Widgets generated from JSON schemas (C++ parser, dynamic widget creation)
* **Creator Tool:** `QTextEdit` with rich text formatting toolbar

**Trade-offs:**
* ⚠️ Limited HTML/CSS support (HTML4 subset, CSS 2.1 only - no CSS3)
* ⚠️ QML learning curve for embedded app authors (different from web technologies)
* ⚠️ Limited WYSIWYG editing capabilities (cannot preview CSS3)
* ✅ **No rendering flash issues** (synchronous rendering)
* ✅ Better performance (faster startup, lower memory)
* ✅ Simpler deployment (no Chromium dependency)
* ✅ Native Qt integration (consistent styling, event handling)

=== Alternative: **Qt WebEngine with Mitigations**

**If WebEngine is chosen despite rendering issues:**

**Mitigation Strategies:**
1. **Pre-rendering:** Pre-render content with new theme before switching
2. **Fade Transitions:** Use fade effects to mask flash
3. **Theme Caching:** Cache common theme combinations
4. **User Education:** Document that theme changes may cause brief flash
5. **Minimize Theme Changes:** Design UI to minimize theme switching frequency

**Risk:** ⚠️ **HIGH** - Rendering flash may be unfixable due to WebEngine architecture

== Final Answers to Discussion Questions

1. ✅ **Embedded Applications:** Qt Quick/QML is acceptable alternative - **CONFIRMED VIABLE**

2. ✅ **Deployment Constraints:** Non-issue - **CONFIRMED**

3. ✅ **Other Project Issues:** Theme change rendering flash - **CRITICAL ISSUE IDENTIFIED**

4. ❌ **Rendering Flash Acceptability:** **UNACCEPTABLE** - Deal-breaker for WebEngine. Other project spent significant effort trying to resolve and was unsuccessful.

5. ✅ **Form Architecture:** Qt Widget Forms acceptable - **CONFIRMED**

6. ✅ **HTML/CSS Limitations:** HTML4 + basic CSS sufficient - **CONFIRMED ACCEPTABLE**
   * Qt has **no plans** to add HTML5/CSS3 support to QTextDocument
   * QTextDocument supports HTML4 subset and CSS 2.1
   * Advanced CSS3 features (flexbox, grid, transforms) will not be available
   * With robust WYSIWYG authoring environment, main loss is advanced CSS3 functionality

7. ✅ **QML vs JavaScript:** QML acceptable if it provides same functionality - **CONFIRMED ACCEPTABLE**
   * QML provides similar interactive capabilities to JavaScript
   * Supports JavaScript expressions for logic
   * Different syntax but equivalent functionality
   * Better Qt integration than JavaScript SPAs

8. ⚠️ **WYSIWYG Limitations:** Depends on HOW limited - **REQUIRES ASSESSMENT**
   * QTextEdit provides rich text editing with formatting toolbar
   * Supports: bold, italic, underline, font size, font family, colors, lists, alignment
   * Limited HTML support (HTML4 subset)
   * Cannot preview CSS3 features
   * May need custom implementation for advanced features

== Final Decision

=== Decision: **QTextDocument + Qt Quick/QML**

**Status:** ✅ **ACCEPTED**

**Rationale Summary:**

1. **Rendering Flash:** UNACCEPTABLE - Deal-breaker for WebEngine
2. **QML Alternative:** Acceptable - Provides equivalent functionality to JavaScript
3. **HTML/CSS Level:** HTML4 + CSS 2.1 sufficient - Qt has no plans for HTML5/CSS3
4. **Forms:** Qt Widget Forms acceptable
5. **WYSIWYG:** Limited but acceptable with robust authoring environment

**Implementation:**
* Content: `QTextBrowser` for HTML4 + CSS 2.1 rendering
* Embedded Apps: QML via `QQuickWidget`
* Forms: Qt Widgets generated from JSON schemas
* Creator Tool: `QTextEdit` with rich text toolbar

== Next Steps

1. ✅ **Decision Made:** QTextDocument + QML architecture accepted
2. **ADR Created:** Architecture Decision Record documented
3. **Implementation Planning:**
   * Redesign `ReaderViewWindow` to use `QTextBrowser`
   * Redesign `ContentEditor` to use `QTextEdit`
   * Design QML embedding mechanism
   * Design Qt Widgets form generator
4. **Proof of Concept:**
   * Create QML embedded app example
   * Test QML embedding in content pages
   * Verify no rendering flash with theme changes
   * Test Qt Widgets form generation
5. **Implementation:** Proceed with content rendering implementation

== Critical Decision Factors

=== Factor 1: Theme Change Rendering Flash

**Status:** ⚠️ **CRITICAL ISSUE**

**Impact:** 
* May be unfixable due to WebEngine architecture
* Creates poor user experience
* Application shell renders before content (appears broken)

**Decision Weight:** **HIGH** - This could be a deal-breaker for WebEngine

=== Factor 2: Embedded Application Technology

**Status:** ✅ **ALTERNATIVE AVAILABLE**

**QML Option:**
* Viable alternative to JavaScript SPAs
* Better Qt integration
* No separate process (no async issues)
* Still supports JavaScript expressions

**Decision Weight:** **MEDIUM** - QML is acceptable, but requires different authoring

=== Factor 3: HTML/CSS Support Level

**Status:** ⚠️ **REQUIRES CLARIFICATION**

**QTextDocument Limitations:**
* HTML4 subset only
* Basic CSS only (no CSS3: flexbox, grid, transforms, etc.)

**Question:** Is full HTML5/CSS3 required, or is HTML4 + basic CSS sufficient?

**Decision Weight:** **HIGH** - Determines if QTextDocument is viable

== References

* Qt WebEngine: https://doc.qt.io/qt-6/qtwebengine-index.html
* QTextDocument: https://doc.qt.io/qt-6/qtextdocument.html
* QTextBrowser: https://doc.qt.io/qt-6/qtextbrowser.html
* Architecture Notes: `architecture-notes.adoc`
* SRS Requirements: `srs.adoc` FR-2.2.x, FR-2.7.x, FR-WE-5.x
* DDD Section 5: Qt WebEngine Integration

